# Network Automation Executor

**Network Automation Executor** — это консольное приложение на Go для автоматизированного управления сетевым оборудованием через SSH/Telnet. Оно позволяет выполнять набор команд на удалённых устройствах, обрабатывать вывод, сохранять результаты и реализует сложную логику выполнения через JSON-задания.

---

## 🎯 Основные возможности

- **Подключение по SSHv1/v2 и Telnet** к сетевым устройствам
- **Автоматическое определение типа оборудования** по строке приглашения
- **Выполнение многоуровневых заданий** с поддержкой условий, циклов, переменных и шаблонов
- **Обработка ошибок** на основе стандартных выводов оборудования
- **Сохранение выводов команд** в файлы для последующего анализа
- **Поддержка регулярных выражений** для фильтрации и генерации подзаданий
- **Логирование и отладка** выполнения заданий

---

## ✅ Поддерживаемые устройства

Executor автоматически определяет тип оборудования по строке приглашения и поддерживает следующих вендоров:

| Вендор | Модели/ОС | Статус |
|--------|-----------|--------|
| **Cisco** | IOS, IOS-XE, NX-OS, ASA, ASAv | ✅ Полная поддержка |
| **Huawei** | VRP, NE Series | ✅ Полная поддержка |
| **F5 Networks** | BIG-IP (TMSH, Bash) | ✅ Полная поддержка |
| **Radware** | Alteon OS | ✅ Полная поддержка |
| **Другие устройства** | С поддержкой SSH/Telnet CLI | ✅ Совместимость через кастомные промпты |
| **Juniper** | JunOS | 🚧 Базовая поддержка |
| **Cisco ASAv Hypervisor** | Hypervisor CLI (NX-OS style) | 🚧 Базовая поддержка |

---

## 📦 Установка

### Сборка из исходников (требуется Go 1.16+)

```bash
git clone https://github.com/yourusername/network-automation-executor.git
cd network-automation-executor
go build -o executor ./cmd
```

### Загрузка готового бинарника

Готовые сборки для Linux, Windows и macOS доступны в [Releases](https://github.com/yourusername/network-automation-executor/releases).

---

## 🚀 Быстрый старт

### Базовое использование

```bash
./executor -t task.json -o ./outputs
```

Где:
- `-t` — путь к JSON-файлу с заданием
- `-o` — директория для сохранения выводов (будет создана, если не существует)

### Пример простого задания

**task.json:**
```json
{
  "host": "192.168.1.1",
  "tasks": [
    {
      "command": "terminal pager 0",
      "params": {
        "commandRepeatAllowed": true
      }
    },
    {
      "command": "show version",
      "params": {
        "outputFile": "show_version.txt"
      }
    }
  ]
}
```

### Запуск с переменными окружения

```bash
export CLI_USERNAME=admin
export CLI_PASSWORD=secret
./executor -t task.json -o ./outputs
```

### Запуск в режиме отладки

```bash
./executor -t task.json -o ./outputs -d
```

---

## 📝 Формат задания

Задание описывается в JSON и поддерживает:

### Базовые поля

```json
{
  "host": "10.0.0.1",
  "vendor": "cisco",
  "variables": {
    "show_cmd": "show running-config"
  },
  "tasks": [
    {
      "command": "{{show_cmd}}",
      "name": "get_config",
      "params": {
        "outputFile": "config.txt",
        "timeout": 30
      }
    }
  ]
}
```

### Условия выполнения

```json
{
  "tasks": [
    {
      "command": "show version",
      "name": "check_version"
    },
    {
      "when": [
        {
          "name": "check_version",
          "ifOutputContains": "Cisco Adaptive Security Appliance"
        }
      ],
      "command": "show failover"
    }
  ]
}
```

### Регулярные выражения для генерации подзаданий

```json
{
  "tasks": [
    {
      "command": "show context detail",
      "params": {
        "filter": "Context\\s\\\"(?P<context>[^\\s\\t]+)\\\"",
        "filterExclude": "([Nn]ull|[Ss]ystem)"
      },
      "tasks": [
        {
          "command": "changeto context {{context}}",
          "tasks": [
            {
              "command": "show route",
              "params": {
                "outputFile": "{{context}}_routes.txt"
              }
            }
          ]
        }
      ]
    }
  ]
}
```

---

## ⚙️ Параметры командной строки

| Параметр | Описание | Обязательный |
|----------|----------|--------------|
| `-t, --task` | Путь к файлу задания в формате JSON | Да |
| `-o, --output` | Директория для сохранения выводов | Да |
| `-d, --debug` | Включить режим отладки (подробный вывод) | Нет |
| `-h, --help` | Показать справку | Нет |

---

## 🔐 Аутентификация

Используются два метода аутентификации:

1. **Через переменные окружения** (рекомендуется):
   ```bash
   export CLI_USERNAME=admin
   export CLI_PASSWORD=password
   ```

2. **Через параметры в задании** (менее безопасно):
   ```json
   {
     "host": "10.0.0.1",
     "variables": {
       "_username": "admin",
       "_password": "password"
     }
   }
   ```

---

## 📁 Структура проекта

```
network-automation-executor/
├── cmd/                    # Точка входа
├── internal/              # Основная логика
│   ├── controller/        # Контроллер выполнения заданий
│   ├── spawner/          # Модуль подключения к устройствам
│   ├── domains/          # Бизнес-сущности
│   └── services/         # Сервисный слой
├── demotasks/            # Примеры заданий
├── outputs/              # Выводы (создаётся при запуске)
└── README.md
```

---

## 🧪 Примеры заданий

В папке `demotasks/` находятся готовые примеры:

1. **basic_show_commands.json** — базовые команды для сбора информации
2. **conditional_check.json** — пример использования условий
3. **context_aware_collection.json** — сбор данных по контекстам на межсетевых экранах
4. **multi_vendor_support.json** — работа с оборудованием разных вендоров

---

## 🔍 Отладка и логирование

При запуске с флагом `-d` приложение выводит:

- Ход выполнения каждого этапа задания
- Отправленные команды и полученные ответы
- Информацию о смене режимов оборудования
- Предупреждения и ошибки

Логи сохраняются в указанной выходной директории.

---

## ⚠️ Ограничения

- Работает только в Linux-окружении
- Требует доступа к устройствам по SSH/Telnet
- Для некоторых старых устройств может потребоваться настройка алгоритмов шифрования
- Размер выводов ограничен доступной памятью

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функциональности
3. Закоммитьте изменения
4. Отправьте Pull Request

---

## 📄 Лицензия

MIT License. Подробнее в файле LICENSE.

---

## 📬 Контакты и поддержка

- Issues: [GitHub Issues](https://github.com/yourusername/network-automation-executor/issues)
- Документация: [Wiki](https://github.com/yourusername/network-automation-executor/wiki)
- Примеры: [demotasks/](demotasks/)

---
