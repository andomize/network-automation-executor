Вот сокращенное описание проекта в формате README.md, которое можно прочитать за 2–3 минуты:

---

# Network Automation Broker

**Network Automation Broker** — это программный комплекс для автоматизированного управления сетевым оборудованием (коммутаторы, маршрутизаторы, межсетевые экраны) через SSH/Telnet.  
Система предназначена для выполнения массовых конфигурационных задач, сбора данных и оркестрации команд без ручного вмешательства.

---

## 🎯 Основные возможности

- **Автоматизация выполнения команд** на множестве устройств через SSHv1/v2 и Telnet
- **Поддержка оборудования различных вендоров** (Cisco, Huawei, F5, Radware и др.)
- **Гибкие задания в формате JSON** с поддержкой условий, циклов, переменных и шаблонов
- **Очередь заданий** с контролем состояния и ограничением одновременных подключений
- **REST API** для интеграции с внешними системами оркестрации
- **Сохранение выводов команд** в файлы для последующего анализа
- **Обработка ошибок** на основе стандартных выводов оборудования

---

## 🧩 Компоненты системы

1. **Оркестратор** — внешняя система, формирующая задания.
2. **Брокер очередей (QBroker)** — принимает задания через REST API, управляет очередью и запускает выполнение.
3. **Выполняющий модуль (Executor)** — подключается к устройству, выполняет команды, обрабатывает ответы.
4. **Устройство** — сетевое оборудование, управляемое через CLI.

---

## 📁 Пример задания (JSON)

```json
{
  "host": "10.0.0.1",
  "tasks": [
    {
      "command": "show version",
      "params": {
        "outputFile": "version.txt"
      }
    }
  ]
}
```

Поддерживаются:
- Условные выполнения (`when`)
- Регулярные выражения для фильтрации вывода
- Генерация подзаданий на основе вывода
- Системные и пользовательские переменные

---

## 🚀 Быстрый старт

### Запуск брокера (QBroker)

```bash
export QB_HTTP_TOKEN=your_token
export QB_PATH_EXECUTOR=/path/to/executor
./qbroker
```

### Запуск выполняющего модуля (Executor)

```bash
./executor -t task.json -o ./outputs
```

---

## 🔧 Технологии

- Язык: **Go (Golang)**
- Протоколы: **SSHv1/v2, Telnet**
- Формат обмена: **JSON**
- API: **RESTful HTTP**
- Хранение: **Файловая система** (без СУБД)

---

## 📌 Ограничения

- Один брокер обслуживает одно устройство одновременно
- Executor работает только под Linux
- Минималистичный стек технологий (без внешних брокеров и БД)
- Требуется доступ к оборудованию по SSH/Telnet

---

## 📄 Документация

Подробное описание формата заданий, REST API и примеры использования находятся в папке `docs/`.

---

## 📬 Контакты

Проект разработан для автоматизации сетевых операций в крупных инфраструктурах.  
Для вопросов и предложений создавайте issue в репозитории.

---

Это описание покрывает основные аспекты проекта: цель, возможности, архитектуру, примеры и инструкции по запуску, сохраняя краткость и информативность.
